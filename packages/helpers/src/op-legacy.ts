export const buildProveWithdrawal = async (
  chainId: number,
  transactionHash: string,
  l1ChainId: number
) => {
  // Do something!
  console.log('I WAS HERE!')
  console.log(chainId, transactionHash, l1ChainId)
}
