// AUTO-GENERATED FILE. DO NOT EDIT.

import earliestBlocks from '../earliestBlocks.json'

interface EarliestBlocks {
  [key: string]: number
}
const typedEarliestBlocks: EarliestBlocks = earliestBlocks

import config_abstract from './abstract'
import config_ancient8 from './ancient8'
import config_arbitrumNova from './arbitrumNova'
import config_base from './base'
import config_baseSepolia from './baseSepolia'
import config_bob from './bob'
import config_cyber from './cyber'
import config_ethereum from './ethereum'
import config_hemi from './hemi'
import config_lisk from './lisk'
import config_mint from './mint'
import config_optimism from './optimism'
import config_redstone from './redstone'
import config_sepolia from './sepolia'
import config_soneium from './soneium'
import config_swell from './swell'
import config_zero from './zero'
import config_zksync from './zksync'
import config_zora from './zora'

export const abstract = {
  slug: 'abstract',
  ...config_abstract,
  earliestBlock: typedEarliestBlocks['abstract'],
}

export const ancient8 = {
  slug: 'ancient8',
  ...config_ancient8,
  earliestBlock: typedEarliestBlocks['ancient8'],
}

export const arbitrumNova = {
  slug: 'arbitrumNova',
  ...config_arbitrumNova,
  earliestBlock: typedEarliestBlocks['arbitrumNova'],
}

export const base = {
  slug: 'base',
  ...config_base,
  earliestBlock: typedEarliestBlocks['base'],
}

export const baseSepolia = {
  slug: 'baseSepolia',
  ...config_baseSepolia,
  earliestBlock: typedEarliestBlocks['baseSepolia'],
}

export const bob = {
  slug: 'bob',
  ...config_bob,
  earliestBlock: typedEarliestBlocks['bob'],
}

export const cyber = {
  slug: 'cyber',
  ...config_cyber,
  earliestBlock: typedEarliestBlocks['cyber'],
}

export const ethereum = {
  slug: 'ethereum',
  ...config_ethereum,
  earliestBlock: typedEarliestBlocks['ethereum'],
}

export const hemi = {
  slug: 'hemi',
  ...config_hemi,
  earliestBlock: typedEarliestBlocks['hemi'],
}

export const lisk = {
  slug: 'lisk',
  ...config_lisk,
  earliestBlock: typedEarliestBlocks['lisk'],
}

export const mint = {
  slug: 'mint',
  ...config_mint,
  earliestBlock: typedEarliestBlocks['mint'],
}

export const optimism = {
  slug: 'optimism',
  ...config_optimism,
  earliestBlock: typedEarliestBlocks['optimism'],
}

export const redstone = {
  slug: 'redstone',
  ...config_redstone,
  earliestBlock: typedEarliestBlocks['redstone'],
}

export const sepolia = {
  slug: 'sepolia',
  ...config_sepolia,
  earliestBlock: typedEarliestBlocks['sepolia'],
}

export const soneium = {
  slug: 'soneium',
  ...config_soneium,
  earliestBlock: typedEarliestBlocks['soneium'],
}

export const swell = {
  slug: 'swell',
  ...config_swell,
  earliestBlock: typedEarliestBlocks['swell'],
}

export const zero = {
  slug: 'zero',
  ...config_zero,
  earliestBlock: typedEarliestBlocks['zero'],
}

export const zksync = {
  slug: 'zksync',
  ...config_zksync,
  earliestBlock: typedEarliestBlocks['zksync'],
}

export const zora = {
  slug: 'zora',
  ...config_zora,
  earliestBlock: typedEarliestBlocks['zora'],
}
